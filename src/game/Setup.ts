import { designConfig } from '../utils/designConfig';

export class Setup {
    public coinWidth = designConfig.sixContent.coinWidth;
    public gap = designConfig.sixContent.gap;
}

export const setup = new Setup();
